ci:
  collect:
    numberOfRuns: 1
    # staticDistDir: build
    startServerCommand: yarn start:web:testing
    settings:
      chromeFlags: --disable-web-security

  assert:
    preset: lighthouse:no-pwa
    assertions:
      aria-valid-attr-value:
        - warn
      csp-xss:
        - warn
      errors-in-console:
        - warn
      meta-viewport:
        - warn
      unused-javascript:
        - warn

  upload:
    # upload options here

  server:
    # server options here

  wizard:
    # wizard options here
