ci:
  collect:
    staticDistDir: build

  assert:
    preset: lighthouse:no-pwa
    assertions:
      aria-valid-attr-value:
        - warn
      csp-xss:
        - warn
      errors-in-console:
        - warn
      meta-viewport:
        - warn
      unused-javascript:
        - warn

  upload:
    # upload options here

  server:
    # server options here

  wizard:
    # wizard options here
