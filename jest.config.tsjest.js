// This file is not used by any configuration, this is just a sample configuration for ts-jest
// For react-native, we use another config to load ts-jest, for react-native-web, we still need to find how to
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
}
